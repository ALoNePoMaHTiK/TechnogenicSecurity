<UserControl x:Class="TechnogenicSecurity.Views.PlanEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm ="clr-namespace:TechnogenicSecurity.ViewModels" 
             mc:Ignorable="d" >
    <UserControl.DataContext>
        <vm:PlanEditViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Image x:Name="Plan" Source="./Images/test.jpg" Width="200" Height="200">
            <Image.InputBindings>
                <MouseBinding Gesture="LeftClick" Command="{Binding GetPositionCommand}" CommandParameter="{Binding ElementName=Plan}"/>
            </Image.InputBindings>
        </Image>
        <TextBlock Text="{Binding Position}"></TextBlock>
    </Grid>
</UserControl>
